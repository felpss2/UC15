import { SafeAreaView } from "react-native-safe-area-context";
import Card from "../components/card";

const Home = () => {
  return (
    <SafeAreaView>
      <Card />
    </SafeAreaView>
  );
};

export default Home;

