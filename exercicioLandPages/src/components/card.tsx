import { Container, ContainerCenter } from "../ts/styles";
import { Text } from "react-native";

const Card = () => {
  return (
    <ContainerCenter>
      <Container></Container>
    </ContainerCenter>
  );
};
export default Card;
