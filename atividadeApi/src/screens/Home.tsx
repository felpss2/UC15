import { SafeAreaView } from "react-native-safe-area-context";
import Header from "../components/header";

const Home = () => {
  return (
    <SafeAreaView>
      <Header />
    </SafeAreaView>
  );
};

export default Home;
